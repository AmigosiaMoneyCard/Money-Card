# Git Workflow Rules

1. **Strictly Push to Staging Only**:
   - All feature development, fixes, tests, and commits must be pushed ONLY to `origin/staging`.
   - **NEVER** push to `origin/main` or merge into `main` unless the user explicitly and directly instructs to push to `main`.
